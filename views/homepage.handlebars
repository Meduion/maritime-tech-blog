{{#each posts as |post|}}
<div class="row py-3 mb-4">
  <div class="column">
    <h2>{{post.title}}</h2>
    <h5>Posted by {{user.name}} on {{post.postDate}}</h4>
    <p>{{post.body}}</p>
  </div>
</div>
{{/each}}